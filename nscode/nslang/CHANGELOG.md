# Changelog

## 0.1.0

- Initial release: TextMate grammar and language configuration for Nano
  Script (`.ns`) — keywords, primitive types, numbers with type suffixes,
  strings, backtick format-string interpolation, comments, and bracket /
  indentation support. `ns.mod` manifests are associated with TOML.
