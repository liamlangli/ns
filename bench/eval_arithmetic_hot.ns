fn main() bool {
    let a = 1
    let b = 3
    let i = 0
    loop i < 5000000 {
        a = (a * 3 + b * 5 + 7) % 1000003
        b = (b + a + i) % 1000003
        i += 1
    }
    return a >= 0 && b >= 0
}
